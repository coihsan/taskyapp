/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'uploadthing.com',
            'utfs.io',
            'subdomain',
        ]
    },
    reactStrictMode: false,
};

export default nextConfig;
