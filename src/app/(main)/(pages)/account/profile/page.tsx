"use client";
import React from 'react'
import { db } from '@/lib/db';

const ProfilePage = () =>{
    return(
        <div>
            Hello, your current active session is
        </div>
    )
}

export default ProfilePage